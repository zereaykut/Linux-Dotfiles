#mainWindow {
    background-color: #191724;
    border-radius: 10px;
    color: white;
    font-family: "Segoe UI";
    font-size: 11pt;
}

#windowLabel {
    background-color: #a1d2da;
    border: 2px solid black;
    padding: 4px;
    border-radius: 6px;
    color: #eb6f92;
}

#gridCell {
    background-color: #191724;
}

#specialCell {
    background-color: #191724;
    border: 2px solid #31748f;
}


/* Style the context menu */
QMenu {
    background-color: #191724;       /* Background of the menu */
    color: #eb6f92;                  /* Text color */
    border: 1px solid #444;          /* Border around the menu */
    padding: 4px;
}

/* Style each item in the menu */
QMenu::item {
    background-color: transparent;  /* Default background for item */
    padding: 6px 20px;              /* Padding inside items */
}

/* Highlighted item (hover or selected) */
QMenu::item:selected {
    background-color: #a1d2da;      /* Highlight color */
    color: #eb6f92;                 /* Text color on hover */
}

/* Optional: disabled menu item */
QMenu::item:disabled {
    color: red;                 /* Color for disabled items */
}

